import { BodyScrollDirective } from './body-scroll.directive';

describe('BodyScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new BodyScrollDirective();
    expect(directive).toBeTruthy();
  });
});
